name: Wiki Tests Automation

on: 
  push:
    branches: [ main ]
  workflow_dispatch: # Це дозволить тобі запускати тест кнопкою вручну

jobs:
  hello-job: # Назва має бути без пробілів
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello
        run: echo "Runner is working!"